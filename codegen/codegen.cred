@includeC("data/format/tokenizer/tokenizer.h")
@includeC("rules.h")

interface codegen {
    int type;
    void register_elem(int type, Token elem);
    void register_subrule(int type, codegen child);
    void debug_print(int depth);//TODO: "this" is too manual, we should pass the entire interface
    bool emit_code();
    codegen transform();
    codegen get_subscope();
}