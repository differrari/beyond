@includeC("data/tokenizer/tokenizer.h")
@includeC("rules.h")

interface codegen_t {
    int type;
    void register_elem(int type, Token elem);
    void register_subrule(int type, codegen_t child);
    void emit_code();
}